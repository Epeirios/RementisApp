import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    header: {
        backgroundColor: 'white',
        padding: 15,
    },
    text: {
        textAlign: 'center',
        color: 'red',
    },
});